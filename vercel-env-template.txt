# Vercel Environment Variables Template
# Copy the values below and use them when prompted by vercel env add
# Or use the add-vercel-env.sh script for interactive setup

# ============================================
# DATABASE
# ============================================
DATABASE_URL=postgresql://user:password@host:5432/pausely?schema=public
# Replace with your actual PostgreSQL connection string

# ============================================
# JWT AUTHENTICATION
# ============================================
JWT_SECRET=your-super-secret-jwt-key-change-in-production
# Generate a strong random string (at least 32 characters)
# You can generate one with: openssl rand -base64 32

JWT_REFRESH_SECRET=your-refresh-token-secret
# Generate another strong random string

JWT_EXPIRES_IN=7d
# Optional: Token expiration time (default: 7d)

JWT_REFRESH_EXPIRES_IN=30d
# Optional: Refresh token expiration (default: 30d)

# ============================================
# OPENAI
# ============================================
OPENAI_API_KEY=sk-your-openai-api-key
# Get from: https://platform.openai.com/api-keys

# ============================================
# STRIPE
# ============================================
STRIPE_SECRET_KEY=sk_live_your-stripe-secret-key
# Get from: https://dashboard.stripe.com/apikeys
# Use LIVE keys for production, TEST keys for development

STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret
# Get from: Stripe Dashboard → Developers → Webhooks
# Create webhook endpoint: https://your-project.vercel.app/api/webhooks/stripe
# Copy the signing secret

NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_your-stripe-publishable-key
# Get from: https://dashboard.stripe.com/apikeys
# This is safe to expose (public key)

STRIPE_PREMIUM_PRICE_ID=price_your-premium-price-id
# Create a product/price in Stripe Dashboard
# Copy the Price ID (starts with price_)

# ============================================
# APP URLs (Optional - Vercel auto-sets)
# ============================================
NEXT_PUBLIC_APP_URL=https://your-project.vercel.app
# Vercel will auto-set this, but you can override

NEXT_PUBLIC_API_URL=https://your-project.vercel.app/api
# Usually: ${NEXT_PUBLIC_APP_URL}/api

# ============================================
# HOW TO ADD TO VERCEL
# ============================================
# Option 1: Use the script
#   chmod +x add-vercel-env.sh
#   ./add-vercel-env.sh

# Option 2: Add manually one by one
#   vercel env add DATABASE_URL
#   vercel env add JWT_SECRET
#   ... (repeat for each variable)

# Option 3: Use Vercel Dashboard
#   1. Go to your project on vercel.com
#   2. Settings → Environment Variables
#   3. Add each variable manually

# ============================================
# IMPORTANT NOTES
# ============================================
# - For Production: Use LIVE Stripe keys
# - For Preview/Development: Can use TEST Stripe keys
# - Generate strong secrets for JWT (use openssl rand -base64 32)
# - Never commit .env files to git (already in .gitignore)
# - After adding variables, redeploy: vercel --prod

